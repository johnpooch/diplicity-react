import * as game from "./getGame"
import * as games from './listGames';
import * as listOptions from './listOptions';
import * as variants from './listVariants';

export const fixtures = {
    game,
    games,
    listOptions,
    variants,
}